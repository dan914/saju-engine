# Database connection (asyncpg DSN)
DATABASE_URL=postgresql+asyncpg://entitlements:entitlements@entitlement-db:5432/entitlements
# Redis cache + rate limiter
REDIS_URL=redis://entitlement-redis:6379/0
# Firebase-style auth configuration for local testing
JWT_AUDIENCE=local-dev
JWT_ISSUER=https://securetoken.google.com/local-dev
# Optional overrides
ENVIRONMENT=development
LOG_LEVEL=INFO
